# run workspace

This pnpm workspace contains the [`run`](packages/run) package.

## Development

Run commands from the workspace root:

```sh
pnpm install
pnpm build
pnpm test
pnpm type-check
pnpm check
```
